const {
    signUp,
    logIn
} = require("./signup-login")
const updateProfile = require("./updateProfile")
const {
    addProduct
} = require('./products')

module.exports = {
    signUp,
    logIn,
    updateProfile,
    addProduct
}